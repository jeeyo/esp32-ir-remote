name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    uses: esphome/workflows/.github/workflows/build.yml@main
    with:
      files: samples/gree-ac-remote/ac-remote.yaml
      esphome-version: latest
      release-version: ${{ github.ref_name }}
      release-summary: "ESP32 IR Remote — Gree AC controller sample built on the beep_detector component"
      release-url: https://github.com/jeeyo/esp32-ir-ac-thermostat
      combined-name: ac-remote

  upload:
    needs: build
    uses: esphome/workflows/.github/workflows/upload-to-gh-release.yml@main
    permissions:
      contents: write
    with:
      version: ${{ needs.build.outputs.version }}
